{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
    <div>
        <h1> Users</h1>
        <ul>
            {% for user in users %}
                <li>
                    {{ user.name }}
                    {{ user.age }}
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
